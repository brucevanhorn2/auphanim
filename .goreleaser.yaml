version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: auphanim
    main: .
    binary: auphanim
    env:
      - CGO_ENABLED=0  # pure Go; no C dependencies
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      # Stamp the git tag into the version variable at link time.
      - -s -w -X main.version={{.Version}}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - auphanim.json.example

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"

homebrew_casks:
  - name: auphanim
    binaries:
      - auphanim
    repository:
      owner: brucevanhorn2
      name: homebrew-tap
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/brucevanhorn2/auphanim"
    description: "TUI developer tool for watching system-wide changes while testing full-stack software"
